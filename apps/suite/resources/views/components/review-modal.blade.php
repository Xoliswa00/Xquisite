{{--
    Legacy component — kept for window event compatibility only.
    The feedback form now lives at /feedback (route: reviews.create).
    This component no longer auto-opens or shows a floating trigger.
--}}
